---
date: 2026-07-13
topic: software-delivery-ops
scan_window_start: 2026-07-13T00:00:00+08:00
scan_window_end: 2026-07-13T23:59:59+08:00
run_status: partial
signal_count: 1
---

# 2026-07-13 信号记录

## `sdo-20260711-gh-aw-sandbox-disclosure` — GitHub Agentic Workflows 将沙箱与 AI 作者披露写入工作流契约

- 记录状态：active
- 主分类：流水线编排与调度
- 横向标签：CI/CD Agent、GitHub Actions、沙箱、Safe Outputs、最小权限、审计、自托管 Runner
- 自主程度：L3 受控执行
- 证据状态：早期信号
- 最高来源优先级：P1
- 相关性：高（直接涉及 Agentic CI/CD 工作流的执行隔离、输出治理、身份和失败可见性）
- 可信度：中（功能由官方 Release 明确披露，但版本仍标记为 Pre-release，尚无独立生产验证）
- 新颖性：高（把 gVisor 运行时、可复用沙箱挂载和 AI 作者披露组合为可执行的工作流控制面）
- 时效属性：background
- 事件/内容发布时间：2026-07-11T08:56:00Z 至 2026-07-22T16:04:21Z
- 首次发现时间：2026-07-13T23:45:00+08:00
- 独立支持来源数：1
- 首次观察时间：2026-07-13T23:45:00+08:00
- 最近有效更新时间：2026-07-23T09:46:51+08:00
- 有效更新次数：8
- 关注度动量：升温
- 相关既有信号：`sdo-20260602-github-review-actions`、`sdo-20260630-harness-worker-agents`

### 信号

GitHub 在 `gh-aw` v0.82.8 Pre-release 中加入可选的 gVisor Agent 沙箱运行时、可由共享工作流片段声明的沙箱挂载，以及用于在 PR 评论和 Issue 中披露 AI 作者身份的 `disclosure-header` Safe Output。该版本还把 OAuth 或缺失令牌失败传递到结论 Job，并修正 confused-deputy 检查范围与自托管 ARC/DinD Runner 的相关问题。

`2026-07-15` 更新：v0.82.9 Pre-release 进一步加入 `docker-sbx` 沙箱目标、`private-to-public-flows: allow` 显式跨可见性授权、阻止 `toJSON(secrets)` 的编译检查、未限定 `push` 分支的扇出检查，以及 `pull_request` 向 `pull_request_target` 的权限升级检测；CLI proxy 默认策略也改为阻止公共仓库上下文访问私有仓库。以上是官方 Release 与已合并 PR 可确认的实现事实，不代表这些 Preview 机制已经过独立生产验证。

`2026-07-17` 更新：v0.82.10 将 strict security mode 设为默认，取消自动 `sudo` 与 `--enable-host-access`，需要显式选择；同时引入声明式 engine/frontmatter 与 workflow evaluation，首批 10 个工作流通过 BinEval 运行并把结果写入 conclusion usage artifact 和每日评估报告。v0.82.11 将评估扩到额外 24 个工作流；`close`、`assign_to_agent` 等工具默认要求 `issue-intent`，Safe Outputs client token 与 project token 分离，PR 输出使用 fork-backed safe path，并对调用次数上限快速失败。以上版本仍为 Pre-release。

`2026-07-18` 更新：v0.82.12 修复两处此前未覆盖的安全边界。第一，编译器原先只把 `${{ secrets.* }}` 识别为需从 Agent sandbox 排除的凭据环境变量，经 `${{ needs.JOB.outputs.OUTPUT }}` 传入的临时 token 会完整暴露给容器；新版在 `mcp-scripts`、`engine.env` 与 `agent.env` 中统一生成 `--exclude-env`。第二，external threat detection 重建 EngineConfig 时曾丢失 custom OpenAI/Anthropic base URL 与新增 allowlist，导致自定义环境下出现 false negative；新版合并主 engine env 并扩大 allowlist 传播条件。官方 Release 与已合并 PR 可确认缺口和修复，但版本仍为 Pre-release。

`2026-07-19` 更新：v0.82.13 修复 Safe Outputs 审计计数长期错误为 `0` 的三个实现缺陷：`run_summary.json` 字段命名不一致、双文件 artifact 没有 flatten，以及 cache backfill 只改内存而未重新持久化。它同时把 `set_issue_type`、`set_issue_field` 与 `add_labels` 的 intent metadata 改为默认保留，但默认仍兼容纯字符串；只有显式 `issue-intent: true` 才进入 strict mode，显式 `false` 可退回旧路径。官方 Release 与已合并 PR 可确认缺陷和修复，但不能把“默认保留 metadata”表述为“默认强制结构化意图”。

`2026-07-20` 更新：四条已合并 PR 补齐或暴露了执行边界。ARC/DinD 的 CLI 安装增加 `--rootless`，可写入 `~/.local/bin` 并适配 runner 的 `allowPrivilegeEscalation: false`；firewall v0.27.37 补充 rootless PATH 与 `ANTHROPIC_AUTH_TOKEN` credential isolation。另两项 Safe Outputs 修复显示，容器外 bridge 因 UID/HOME 不同曾触发 Git `dubious ownership` 并静默只读；Agent 已交付 terminal output 后的 late activity 也曾被 watchdog 判为失败，重试四次后反而得到零输出。实现已合并但尚未进入新 Release，独立支持来源数不增加。

`2026-07-21` 更新：`gh-aw` v0.82.14 成为 Latest Release，将此前逐条合并的 rootless 安装和 Safe Outputs 可靠性变化纳入正式版本，并新增三组发布级控制：64 条 Agentic Workflows 启用 evals；DeepSec 与 VulnHunter 作为按需及每日安全扫描工作流；Safe Output mutation tools 按运行模式给出 intent guidance，编译器拒绝 schema 变化后未重建的工作流。版本还处理慢 Runner 上 gateway 先于工具服务超时、terminal safe-output 被误判失败等边界。官方 Release 能确认这些实现，尚不能证明评测阈值、扫描效果或生产采用。

`2026-07-23` 更新：`gh-aw` v0.83.0 Pre-release 把工作流失败语义、安全固定和健康分类进一步下沉到编译与运行控制。新增静态规则识别 `setFailed` 后零退出、同步进程与文件 I/O 缺少异常处理等失败掩盖模式；运行时拒绝空 Action pin SHA，并把 Docker build action 固定到 Commit SHA。健康指标区分零轮 `driver-exit` 与 Agent 逻辑失败，OTLP 在缺少结论信号时也会把非零 Copilot CLI 退出记为失败；Anthropic SDK 内层重试被禁用，以避免 API 不可靠时形成终端错误风暴。官方 Release 可确认这些实现，但版本仍为 Pre-release，尚无独立生产验证。

### 为什么重要

这表明 Agentic CI/CD 的治理机制正从提示词约定下沉到可编译、可复用和可审计的工作流字段：执行隔离、挂载范围、输出通道、身份失败和作者披露可以成为流水线契约的一部分。它与 Harness 将 Worker Agent 纳入 RBAC、OPA、审批和审计的基线信号形成跨产品呼应，但当前不足以据此上升为成熟行业趋势。

`2026-07-17` 分析判断：控制面开始同时覆盖“默认拒绝”的运行权限、面向副作用工具的意图门禁、输出令牌隔离与持续评估；这使安全默认值和行为回归测试成为 Agentic Workflow 的发布契约，而不只是部署建议。

`2026-07-18` 分析判断：两项修复同时说明“声明了沙箱/威胁检测”不等于所有凭据来源和自定义 provider 路径都已被隔离。修复本身增强了控制面，但此前可见的 credential 与 false-negative 路径削弱了对 Pre-release 安全完整性的判断，因此关注度动量记为“降温”。

`2026-07-19` 分析判断：写操作意图元数据覆盖面扩大值得肯定，但审计总数长期为零说明控制面的“可审计”仍依赖数据生成、artifact 解析和持久化链路共同正确。修复没有增加独立生产证据，且 strict enforcement 仍需显式开启，因此维持“早期信号”和“降温”。

`2026-07-20` 分析判断：rootless 关闭了 runner 容器内强制提权的兼容性缺口，但 DinD sidecar 仍可能保持 privileged，Docker socket 边界也未消失。两个 Safe Outputs 缺陷进一步说明控制面正确性横跨容器身份、Git workspace ownership、桥接进程和终态语义；新证据实质扩展了已知边界，动量改为“持续”，证据状态仍为“早期信号”。

`2026-07-21` 分析判断：正式版本把行为评测、安全扫描和副作用输出契约集中成发布门禁，且确认前一日的 rootless 与终态修复已经进入 Release；这比单项修复更能改变对控制面完整性的判断，因此动量记为“升温”。但评测与扫描的存在不等于阈值、样本和误报已满足生产要求，主证据状态仍维持“早期信号”。

`2026-07-23` 分析判断：失败语义开始贯穿生成时 lint、运行时退出和遥测结论，供应链固定与 SDK 重试也被纳入同一发布控制面；这扩展了既有沙箱和 Safe Outputs 信号的可靠性边界，但 Pre-release 仍不足以证明生产工作流的误报率、恢复效果或整体供应链安全。

### 尚待确认

- v0.82.8 何时进入稳定版，以及 gVisor 与 ARC/DinD 组合在生产环境中的兼容性和成本。
- `disclosure-header` 是否会被组织政策、审计工具或代码评审流程实际采用。
- Safe Outputs、网络白名单和挂载配置能否覆盖提示注入、凭据滥用及跨仓库副作用等主要风险。
- strict mode、`issue-intent` 与 fork-backed PR 在大型真实工作流中的兼容性、误阻断率和绕过面。
- BinEval 的样本、评分准则、失败处置与版本回归阈值是否足以支撑生产门禁。
- v0.82.11 及以前经 job output 传入凭据的实际暴露范围、轮换需求，以及其他表达式来源是否仍可能绕过 `--exclude-env`。
- external threat detection 在自定义 provider、代理和动态 allowlist 组合下的回归覆盖与 fail-closed 行为。
- v0.82.13 修复前 Safe Outputs 审计计数错误影响了哪些报告、时间范围和合规判断，是否需要历史回填或更正。
- `issue-intent` 省略、显式 `true` 与显式 `false` 三种模式在组织级策略中如何被强制，纯字符串兼容路径是否会削弱意图门禁。
- `--rootless`、firewall v0.27.37 与 Safe Outputs 修复何时进入稳定 Release，以及 ARC/DinD 的 privileged sidecar 和 Docker socket 风险如何约束。
- Safe Outputs bridge 在不同 UID/HOME、跨仓库 checkout 和 self-hosted runner 组合下是否 fail closed，而不是静默退回只读。
- 已有 terminal safe-output 时，迟到动作、错误与副作用如何被保留，避免把不完整交付误判为成功。
- v0.82.14 的 64 条工作流 evals 使用哪些数据、评分阈值与失败阻断规则，DeepSec/VulnHunter 的扫描权限、误报和发现处置如何治理。
- v0.83.0 的失败掩盖规则误报/漏报、历史运行重分类结果、稳定版时间，以及嵌套重试调整对完成率和延迟的实际影响。

### 支持来源

- P1 · [github/gh-aw v0.82.8](https://github.com/github/gh-aw/releases/tag/v0.82.8) · GitHub · GitHub Pre-release · `2026-07-11T08:56:00Z`
- P1 · [github/gh-aw v0.82.9](https://github.com/github/gh-aw/releases/tag/v0.82.9) · GitHub · GitHub Pre-release · `2026-07-13T16:42:32Z`
- P1 · [github/gh-aw v0.82.10](https://github.com/github/gh-aw/releases/tag/v0.82.10) · GitHub · GitHub Pre-release · `2026-07-16T06:35:14Z`
- P1 · [github/gh-aw v0.82.11](https://github.com/github/gh-aw/releases/tag/v0.82.11) · GitHub · GitHub Pre-release · `2026-07-16T19:50:40Z`
- P1 · [github/gh-aw v0.82.12](https://github.com/github/gh-aw/releases/tag/v0.82.12) · GitHub · GitHub Pre-release · `2026-07-17T07:19:15Z`
- P1 · [Exclude job-output credential env vars from agent sandbox](https://github.com/github/gh-aw/pull/46076) · GitHub 维护者 · 已合并 PR · `2026-07-17T06:53:00Z`
- P1 · [Honor custom engine base URLs and allowlist propagation in external threat detection](https://github.com/github/gh-aw/pull/46077) · GitHub 维护者 · 已合并 PR · `2026-07-17T02:55:08Z`
- P1 · [github/gh-aw v0.82.13](https://github.com/github/gh-aw/releases/tag/v0.82.13) · GitHub · GitHub Pre-release · `2026-07-18T09:17:15Z`
- P1 · [Default-on issue intent metadata for set_issue_type, set_issue_field, and add_labels](https://github.com/github/gh-aw/pull/46207) · GitHub 维护者 · 已合并 PR · `2026-07-18T08:55:23Z`
- P1 · [Populate SafeItemsCount so audits report accurate safe-output write counts](https://github.com/github/gh-aw/pull/46360) · GitHub 维护者 · 已合并 PR · `2026-07-18T09:00:19Z`
- P1 · [Add `--rootless` flag to install Copilot CLI for ARC/DinD runners](https://github.com/github/gh-aw/pull/46047) · GitHub 维护者 · 已合并 PR · `2026-07-19T20:26:52Z`
- P1 · [Bump gh-aw-firewall to v0.27.37 and sync AWF integration artifacts](https://github.com/github/gh-aw/pull/46637) · GitHub 维护者 · 已合并 PR · `2026-07-19T21:16:13Z`
- P1 · [Bridge Git safe.directory trust through process-scoped configuration](https://github.com/github/gh-aw/pull/46057) · GitHub 维护者 · 已合并 PR · `2026-07-19T06:52:32Z`
- P1 · [Treat terminal Safe Output followed by late activity without destructive retries](https://github.com/github/gh-aw/pull/46493) · GitHub 维护者 · 已合并 PR · `2026-07-19T22:09:01Z`
- P1 · [github/gh-aw v0.82.14](https://github.com/github/gh-aw/releases/tag/v0.82.14) · GitHub · 官方 Release · `2026-07-20T17:59:08Z`
- P1 · [github/gh-aw v0.83.0](https://github.com/github/gh-aw/releases/tag/v0.83.0) · GitHub · 官方 Pre-release · `2026-07-22T16:04:21Z`

### 更新历史

- `2026-07-13`：首次记录。
- `2026-07-15`：追加 v0.82.9 的沙箱、跨可见性授权、秘密防泄漏、触发器权限升级检测与仓库可见性默认策略；维持“早期信号”。
- `2026-07-17`：追加 v0.82.10/11 的 strict security 默认值、持续评估、意图门禁、令牌分离、fork-backed PR 与调用上限；维持“早期信号”。
- `2026-07-18`：追加 job-output 凭据曾暴露给 sandbox、custom provider 威胁检测 false negative 及 v0.82.12 修复；维持“早期信号”，动量记为“降温”。
- `2026-07-19`：追加 v0.82.13 的 Safe Outputs 审计计数缺陷修复与 intent metadata 默认语义；维持“早期信号”和“降温”。
- `2026-07-20`：追加 ARC/DinD rootless、firewall 凭据隔离、Safe Outputs 跨进程 Git trust 与终态误判修复；维持“早期信号”，动量改为“持续”。
- `2026-07-21`：追加 v0.82.14 的 64 条工作流评测、按需/每日安全扫描、Safe Outputs 意图提示与 schema fail-fast，并确认此前若干已合并控制进入 Latest Release；维持“早期信号”，动量记为“升温”。
- `2026-07-23`：追加 v0.83.0 的失败掩盖静态规则、Action pin 守卫、运行失败分类与 SDK 重试隔离；维持“早期信号”和“升温”。
